# Watify — SPA Routing
# Coloca este archivo en public_html/ al hacer deploy en Hostinger

Options -MultiViews

RewriteEngine On
RewriteBase /

# Redirige /api/* al directorio api/
RewriteRule ^api/(.*)$ api/$1 [L]

# Si el archivo o directorio existe, servir directamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Todo lo demás al index.html (SPA)
RewriteRule . /index.html [L]
